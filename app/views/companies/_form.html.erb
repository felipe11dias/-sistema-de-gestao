<div class="card">
  <div class="card-body">
    <%= form_with(model: company, local: true) do |form| %>
      <% if company.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

          <ul>
            <% company.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field form-group">
        <%= form.label :name_company, 'Nome da empresa' %>
        <%= form.text_field :name_company, class: "form-control", :required => true %>
      </div>

      <div class="field form-group">
        <%= form.label :username, 'Nome do usuário'  %>
        <%= form.text_field :username, class: "form-control", :required => true %>
      </div>

      <div class="field form-group">
        <%= form.label :name, 'Nome' %>
        <%= form.text_field :name, class: "form-control", :required => true %>
      </div>

      <div class="field form-group">
        <%= form.label :email %>
        <%= form.email_field :email, class: "form-control", :required => true %>
      </div>

      <div class="field form-group">
        <%= form.label :password, 'Senha' %>
        <%= form.password_field :password, class: "form-control", :required => true %>
      </div>

      <div class="actions">
        <%= form.submit 'Criar nova empresa', class: 'btn btn-dark' %>
      </div>
    <% end %>
  </div>
</div>